<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>tc9_pr__Calendar_Period__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <deprecated>false</deprecated>
    <description>Default is null. Updated to the last Calendar Period of the Payee Pay Batch Record during calculations so that it is not calculated for the same period over and over again.</description>
    <inlineHelpText>Default is null. Updated to the last Calendar Period of the Payee Pay Batch Record during calculations so that it is not calculated for the same period over and over again.</inlineHelpText>
    <label>Calendar Period</label>
    <referenceTo>tc9_pr__Calendar_Period__c</referenceTo>
    <relationshipLabel>Payee Leave Rules</relationshipLabel>
    <relationshipName>Payee_Leave_Rules</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
