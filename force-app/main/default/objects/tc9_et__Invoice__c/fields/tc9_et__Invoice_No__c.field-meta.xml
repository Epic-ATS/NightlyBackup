<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>tc9_et__Invoice_No__c</fullName>
    <deprecated>false</deprecated>
    <description>Defines the Invoice No (if Alternative Invoice No is populated then Alternative Invoice Number else Invoice No (Name) with prefix trimmed off)  This is used for on Email Templates</description>
    <externalId>false</externalId>
    <formula>IF(
not(isblank( tc9_et__Alternative_Invoice_No__c )),
tc9_et__Alternative_Invoice_No__c,
Right(Name,10)
)</formula>
    <inlineHelpText>Defines the Invoice No (if Alternative Invoice No is populated then Alternative Invoice Number else Invoice No (Name) with prefix trimmed off)  This is used for on Email Templates</inlineHelpText>
    <label>Invoice No</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
